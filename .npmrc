# @angular/fire@20 peer-deps Angular 20, but this project runs Angular 21.
# The combination works at runtime; this lets `npm install` resolve without --legacy-peer-deps each time.
legacy-peer-deps=true
